---
tags: [sprint-evaluation]
sprint: 4
result: PASS
---

# Sprint 4 評価レポート（試行2回目）

## 総合判定: PASS

## 検証モード: Bash（CLI実行・関数直叩きスクリプト・単体テスト）
- 対象プラットフォームは`web`だが「画面を持たないバックエンド自動化」CLIのため、Playwright（ブラウザ操作）ではなくBashでのCLI実機実行・関数直叩き・出力ファイル確認で検証。仕様上の正しい検証手段であり、環境制約による縮退ではない。

## 基準ごとの結果
| 基準 | 結果 | 根拠 |
|---|---|---|
| 致命的バグ0件 | PASS | 全受け入れ基準を実機CLI・スクリプトで再現し不具合なし。前回FAIL原因(全角疑問符欠落)も修正確認済み |
| コンソールエラー0件 | PASS | `npm run thread`はINFOログのみ。失敗系はWARN+exit1（意図された挙動）。typecheckエラーなし |
| 受け入れ基準充足率100% | PASS | 5基準すべてPASS（下記） |

## 受け入れ基準ごとの検証結果（すべてPASS）
- 文字数計算・境界値: 半角280→単一/281→複数、全角140(重み280)→単一/141(重み282)→複数を実機確認。URL固定重み23も確認。
- 上限内単一/超過分割・各ツイート上限内: 長文分割で全チャンクw<=280、huge(総重み10200)入力でも全ツイートw<=280を確認。
- 文/意味の切れ目+1/N表記+表記込み上限内: 句点区切りで分割、各ツイートに`(N/M)`付与、表記込みでw<=280（例266/166）を確認。
- リンクツイート末尾1件・上限内: `kind=link`が末尾1件のみ、実長420文字URLでも固定重み換算でw=31<=280を確認。
- 極端に長い入力の上限本数: 300回反復入力で本文6本(=MAX_BODY_TWEETS)+リンク1本、6本目に省略記号「…」付与を確認。

## 今回修正点の重点再検証（前回FAIL原因）
- 全角疑問符`？`のみで文が終わる長文（`...のだろうか？...ではない？`×6=総重み432）を投入。
  - 結果: 2チャンクに分割、両チャンクとも`？`で文単位終端（tail: `...のだろうか？` / `...ではない？`）、単語途中の強制分割なし、各w<=280。
- CLIでも8文の全角疑問符文（重み270）が単一ツイートに、より長い版（12反復）が4分割（本文3+リンク、各<=230）で正しく句点区切りされることを確認。
- 正規表現 `/(?<=[。!?！？\n])/` の文字クラスをコードポイント確認: 。(U+3002) !(U+0021) ?(U+003F) ！(U+FF01) ？(U+FF1F) の5文字。重複・欠落なし、全角疑問符U+FF1Fが正しく含まれる。
- `npm test` 56件全pass（全角疑問符の回帰テスト含む）、typecheckエラーなし。

## その他の確認
- 空文字入力: `splitIntoBodyTweets('')`→`[]`、composeThreadはリンク1件のみ返す（クラッシュなし）。
- 単一本文でもリンク別ツイート: body(順序表記なし)+link の2ツイート、本文側にURL非含有。
- 生成失敗(`success:false`)時: WARNログ出力+終了コード1で安全終了（実exit code=1を確認）。
- 出力ファイル`data/output/latest-thread.json`が正しく生成されることを確認。

## 発見したバグ・問題点（FAILの原因）
- なし。

## 軽微な改善点（ブロッカーではない）
- リンクツイート本文は「元記事: {URL}」固定（自己評価どおりSprint8で設定化予定）。
- 絵文字・特殊記号はサロゲートペアを一律全角(重み2)扱いで、実Xのtwitter-text仕様とは厳密一致しない（仕様書の簡略ルール「半角1・全角2換算」には準拠）。本スプリント基準の範囲外。
- 本文の順序表記(N/M)のMは本文本数のみを指し、末尾リンクツイートは総数に含まれない。仕様要求は満たしており許容範囲。

## 未検証項目（実機確認が必要）
- 該当なし（ネイティブ専用機能なし。すべてBashで検証可能）。

## プレビュー画像（PASSかつ画面を持つプロダクトの場合のみ）
- 該当なし（画面を持たないバックエンドCLIのため）。

## 関連ドキュメント
- [[sprint-4-selfeval]]（ジェネレーターの自己評価レポート）
- [[sprint-4-brief]]（本スプリントの仕様抜粋）
