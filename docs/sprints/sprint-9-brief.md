---
tags: [sprint-brief]
sprint: 9
---

# Sprint 9 brief: 外部cronサービスからworkflow_dispatchで起動できる

## 対象プラットフォーム
`web`(画面を持たないバックエンド自動化)。

## 含まれる機能(仕様書「機能一覧」より原文)

### F8. 外部cronサービスからのトリガー連携
- 説明: GitHub Actions の schedule(cron)は遅延・不発が多いため、cron-job.org 等の外部無料サービスから GitHub Actions の workflow_dispatch エンドポイント(REST API)を定時に叩いて起動する。この方式のための起動口と設定手順を提供する。
- 優先度: Must
- 受け入れ基準:
  - [ ] 外部から workflow_dispatch で起動できる実行ワークフローが用意され、手動での workflow_dispatch 発火で投稿処理(またはドライラン)が走ることを確認できる。
  - [ ] ワークフローは「どの投稿枠(朝/昼/夜)か」を入力として受け取れ、外部cron側で枠ごとに別ジョブとして呼び分けられる(または起動時刻から枠を判定できる)。
  - [ ] cron-job.org 等で3枠ぶんのジョブを設定するための手順書(叩くURL・HTTPメソッド・必要な認証ヘッダ・リクエストボディの形・設定すべき時刻)がドキュメントとして提供される。
  - [ ] 認証情報(GitHub PAT等)を手順書に平文で書かず、利用者が自分の値を差し込む前提のプレースホルダで示す。
  - [ ] GitHub Actions の schedule トリガーは主経路にせず、あってもバックアップ用途である旨がドキュメントに明記される。

## このスプリントの受け入れ基準(デモできる状態・仕様書「スプリント計画」より原文)
- [ ] workflow_dispatch で起動できる実行ワークフローが用意され、手動発火で投稿処理(またはドライラン)が走ることを確認できる。
- [ ] ワークフローが投稿枠(朝/昼/夜)を入力として受け取れる、または起動時刻から枠を判定できる。
- [ ] cron-job.org 等で3枠ぶんのジョブを設定する手順書(URL・HTTPメソッド・認証ヘッダ・ボディ・設定時刻)が提供され、認証情報はプレースホルダで示される。
- [ ] schedule トリガーが主経路でなくバックアップ用途である旨がドキュメントに明記される。

## 評価基準(仕様書より原文)
- 致命的バグ: 0件 / コンソールエラー: 0件 / 受け入れ基準充足率: 100%
- 補足: 外部cron-job.org側の実登録は利用者操作のため、検証は「workflow_dispatchが外部HTTP呼び出しで発火する形になっていること」と「手順書の内容が正しいこと」で行う。

## 関連するリスク・留意事項(仕様書より原文抜粋・外部サービス依存)
- **GitHub Personal Access Token(PAT)の発行**: 外部cronサービスから GitHub Actions の workflow_dispatch を叩くために、workflow 起動権限を持つ PAT を利用者本人が発行する必要がある。PAT は有効期限が切れると外部トリガーが不発になるため、期限管理は利用者の責任範囲。
- **cron-job.org(または同等の無料外部cronサービス)の設定**: 定時トリガーの実体として、利用者本人がアカウント登録し、3つの時刻それぞれに対して GitHub の workflow_dispatch エンドポイントを叩くジョブ(URL・HTTPメソッド・認証ヘッダ・リクエストボディ)を設定する必要がある。設定手順は本システムがドキュメントとして提供するが、登録・設定操作自体は利用者が行う。

## 事前確認の結果(オーケストレーターがユーザーに確認済み)
- 配置先リポジトリ: 既存の `github.com/menx8374-source/x-auto-post` を新システムのコードで置き換える方針。手順書の`owner/repo`・workflow_dispatchのURLはこのリポジトリを前提に記述してよい(例: `https://api.github.com/repos/menx8374-source/x-auto-post/actions/workflows/<workflow-id-or-filename>/dispatches`)。
- GitHub PATは過去プロジェクトで既に発行・登録済みのものを再利用する想定(ユーザー側で用意)。cron-job.orgのアカウント登録・ジョブ設定は、この仕様が固まり次第ユーザーが「今すぐ用意する」と回答済み。実際のジョブ設定はユーザー自身がSprint 9完了後の手順書に沿って行う。
